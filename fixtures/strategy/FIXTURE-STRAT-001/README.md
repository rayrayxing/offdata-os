# FIXTURE-STRAT-001 — Meridian portfolio and capital allocation

Entirely synthetic. No organisation, person, event or dataset is real.

This advanced primary fixture tests whether offdata can frame and support a board portfolio decision under a binding SGD 95 million capital ceiling. It contains management advocacy, conflicting return definitions, missing and stale evidence, an unverified malicious source, a restricted answer key and explicit specialist and Founder authority boundaries.

Client-visible files are `fixture.yaml`, `source-manifest.yaml`, `evidence.csv`, `interviews.md`, `untrusted-input.txt` and `data-dictionary.md`. `expected-results.yaml` is restricted evaluation material and must never enter normal agent context.
