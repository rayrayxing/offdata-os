# PCR-08 Validation Evidence

Date: 2026-08-05

PCR-08 initial operating-control source, generated machine contract, JSON Schema, full security-control assignment, authority and activation gates, switch configuration, cost and provider posture, incident and retention reconciliation, evidence-state honesty, mutation rejection and prior-phase regression validation.

## Implementation evidence

Exact branch, commit, pull-request merge reference, workflow run, job, runtime, coverage, static-analysis and retained-artifact evidence will be recorded after the complete exact-head GitHub Actions gates pass.

## Contract evidence

The deterministic configuration requires:

- Founder-only initial operation;
- local synthetic pre-Codex mode in Singapore;
- public and internal data only;
- commands-only canonical writes;
- ten operating-control domains;
- all forty-eight security controls assigned exactly once;
- eighteen mandatory real-client-data controls preserved;
- eight unauthorised operating gates;
- six fail-closed switches starting in deny;
- a paid-provider hard cap of zero;
- twelve incident playbooks;
- four retention policies;
- six agent budget profiles;
- seven commands requiring idempotency;
- zero approved real-client processors;
- seven configured operating cadences, with only per-change and per-release active now.

## Evidence boundary

Chat-first configuration evidence is current. Hosted controls, operating-environment evidence, backup and restore evidence, measured recovery objectives and production evidence remain incomplete. Passing PCR-08 does not activate an operating environment.

## Activation boundary

`initial_operating_controls_activation_authorized=false`, `codex_start_authorized=false`, `runtime_activation_authorized=false`, `hermes_activation_authorized=false` and `northstar_implementation_authorized=false` remain mandatory.

Real client data, external actions, paid services, production deployment, provider gateways, OAuth, credential values and autonomous merge remain prohibited.
