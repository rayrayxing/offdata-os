# Phase 1 CI validation request

This temporary branch contains no contract changes. It exists to trigger the pull-request validation workflow against the current `main` Phase 1 machine-contract baseline.

Final exact-main validation iteration: public policy import-path preservation, no Ruff exception, no MyPy suppression, mandatory lint, strict type checking and 90 percent coverage gate.
