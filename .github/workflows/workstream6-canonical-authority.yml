name: Workstream 6 canonical authority and evidence registry

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: write

jobs:
  validate-canonical-authority:
    name: Validate WS6.4 canonical authority registry and complete prior components
    uses: ./.github/workflows/workstream6-final-launch-control.yml
    permissions:
      contents: read
      issues: read
      pull-requests: write
