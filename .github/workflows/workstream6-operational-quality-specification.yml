name: Workstream 6 operational quality specification

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: write

concurrency:
  group: workstream6-operational-quality-specification-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-operational-quality-specification:
    name: Validate WS6.13 operational quality specification and complete prior components
    uses: ./.github/workflows/workstream6-final-launch-control.yml
    permissions:
      contents: read
      issues: read
      pull-requests: write
