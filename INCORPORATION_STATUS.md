# Incorporation Status

All seven previously identified residual areas are incorporated into executable setup mechanisms.

1. **Exact G0–G10:** Gate Policy Import from actual VentureOS source + deterministic normalization + independent Codex ratification. Missing source becomes `BLOCKED_SOURCE_REQUIRED`, never guessed policy.
2. **Capital/risk:** safe zero-LIVE defaults + owner questionnaire + versioned owner policy.
3. **Business/legal identity:** bootstrap questionnaire + secret provisioning + jurisdiction/sector policy hooks.
4. **EasyCLIProxyAPI:** explicit qualification harness, measured error/quota behavior and tested DeepSeek fallback.
5. **DeepSeek Harness:** pinned replaceable builder subprocess/worktree lane first; future native Hermes worker-lane adapter only after proven useful.
6. **Multi-host HA:** supervised pinned single-host Hermes/Kanban v1 + authoritative PostgreSQL + off-host backups/restore + future explicit broker boundary. Never network-share Kanban SQLite.
7. **Empirical unknowns:** formal model/role/channel/Contrarian/experiment/profitability/calibration programs. These are operating experiments, not architecture omissions.

Remaining inputs are external: the actual VentureOS source, owner risk values, credentials/business identity, legal jurisdiction details and real-world customer/economic outcomes.