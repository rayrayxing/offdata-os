# Claude Code Lane

Claude Code is an alternate builder/reviewer for tasks where independent implementation, refactoring or second-model review is useful. It receives the same BuildContract and has no direct gate/evidence/finance authority. Do not assume Claude Pro implies Anthropic API billing/access; configure the actual available CLI path and measure it separately.